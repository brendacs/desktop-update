import './WindowsDesktop.scss'

// Placeholder for the planned Windows desktop skin. Swap it in from App.tsx's
// OS switch once its apps/windows are built out, mirroring MacDesktop.
export default function WindowsDesktop() {
  return (
    <div className="windows-desktop">
      <p>Windows desktop — coming soon.</p>
    </div>
  )
}
